spring:
  datasource:
    url: jdbc:tc:mysql:8.4.8://localhost:3306/course_repository_test?useSSL=false&serverTimezone=UTC&TC_DAEMON=true
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: root
    password: password
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/course/db.changelog-initial-schema.yaml
